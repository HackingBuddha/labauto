name: labauto-ci
channels:
  - conda-forge
dependencies:
  # --- Conda Packages ---
  - python=3.11
  - pandas
  - pyarrow
  - biopython=1.85
  - scanpy
  - scikit-learn
  - fastapi
  - uvicorn
  - joblib
  - ruff  # <<< THE FIX: Add ruff to the conda dependencies

  # --- Pip Packages ---
  - pip:
    - -r requirements-ci.txt